/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://aureeture.in', // Change this to your actual domain
  generateRobotsTxt: true, // (optional)
  // ...add more options if needed
};